## Packages
framer-motion | Essential for smooth, high-end, executive-level reveal animations and transitions without being overly flashy.

## Notes
- Tailwind configuration will need to rely heavily on CSS variables defined in index.css to enforce the strict color palette (Obsidian Matte Black, Midnight Navy, Royal Metallic Gold).
- The application uses a completely custom dark theme; standard shadcn light/dark toggles are intentionally omitted as this brand is strictly dark mode.
